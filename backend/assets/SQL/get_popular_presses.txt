SELECT
	RL.BODY
FROM
	COMPANY AS CP
	JOIN RELEASE AS RL ON CP.COMPANY_ID = RL.COMPANY_ID
	JOIN RELEASE_STATISTIC AS RS ON RS.COMPANY_ID = RL.COMPANY_ID AND RS.RELEASE_ID = RL.RELEASE_ID
WHERE
	RL.release_type_id = ${release_type_id_int}
ORDER BY
	rs.PAGE_VIEW DESC
LIMIT
	1;